#include <iostream>
#include <vector>
#include <algorithm>


using namespace std;

int int main(int argc, char const *argv[]) {
  int n; cin >> n;
  vector<int> numbers(n)
  vector<int> common;

  for (size_t i = 0; i < n; i++) {
    cin >> numbers[i];
    
  }
  return 0;
}
